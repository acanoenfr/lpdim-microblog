{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block content %}
    <h1>Please sign up</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Sign up</button>
    <a href="{{ path('security_login') }}">Already have an account ?</a>
    {{ form_end(form) }}
{% endblock %}
